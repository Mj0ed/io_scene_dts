schema_version = "1.0.0"

id = "io_scene_dts"
version = "0.4.0"
name = "Torque DTS format"
tagline = "Import and export Torque DTS/DSQ meshes and animations"
maintainer = "port"
type = "add-on"

website = "https://github.com/qoh/io_scene_dts"

tags = ["Import-Export"]

# Ported to the Blender 4.x/5.x Python API (slotted actions, EEVEE Next
# materials, renamed Principled BSDF sockets). 4.2 is the first version with
# the extensions system and these APIs.
blender_version_min = "4.2.0"

license = [
  "SPDX:MIT",
]
